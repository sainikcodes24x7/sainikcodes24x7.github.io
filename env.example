GITHUB_TOKEN=your_token
GITHUB_USERNAME=your_username

# Optional: server-side AI proxy returning JSON in the form {"answer":"..."}.
# Keep provider API keys on that server; never expose them in a REACT_APP variable.
REACT_APP_CHAT_API_URL=https://your-secure-chat-endpoint.example.com/chat
